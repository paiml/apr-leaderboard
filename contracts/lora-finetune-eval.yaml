metadata:
  version: 1.0.0
  created: 2026-04-01
  author: PAIML
  description: >
    End-to-end LoRA fine-tuning evaluation — train on coding data,
    merge adapter, evaluate on HumanEval pass@1.
  references:
    - "Chen et al. (2021) Evaluating Large Language Models Trained on Code"
    - "Dettmers et al. (2023) QLoRA"

equations:
  lora_merge:
    formula: "W_merged = W_base + (α/r) · A @ B"
    domain: "W_base ∈ ℝ^{d_out×d_in}; A ∈ ℝ^{d_out×r}; B ∈ ℝ^{r×d_in}; α, r > 0"
    codomain: "W_merged ∈ ℝ^{d_out×d_in}"
    invariants:
      - "Merged weight shape = base weight shape"
      - "When A=0 or B=0, merged = base (no-op merge)"

  training_loss:
    formula: "L(θ, t) = CE(f_θ(x_t), y_t) averaged over batch"
    domain: "θ = LoRA parameters; t = training step"
    codomain: "L ∈ ℝ≥0"
    invariants:
      - "avg_loss(epoch_N) < avg_loss(epoch_1) for N >= 2"

proof_obligations:
  - type: bound
    property: "Training loss decreases over epochs"
    formal: "avg_loss(last_epoch) < avg_loss(first_epoch)"

  - type: equivalence
    property: "Adapter merge follows LoRA formula"
    formal: "W_merged = W_base + (α/r) · A @ B"

  - type: bound
    property: "Fine-tuned model maintains baseline quality"
    formal: "pass@1(merged) >= pass@1(base) - 2pp"
    tolerance: 0.02

falsification_tests:
  - id: FALSIFY-EVAL-001
    rule: "Training loss decreases over epochs"
    prediction: "avg_loss(last_epoch) < avg_loss(first_epoch)"
    if_fails: "Training diverges or learning rate too high"
  - id: FALSIFY-EVAL-002
    rule: "Adapter merge produces valid .apr file"
    prediction: "apr check merged.apr passes"
    if_fails: "LoRA merge corrupts model format"
  - id: FALSIFY-EVAL-003
    rule: "Merged model maintains baseline quality"
    prediction: "pass@1(merged) >= 83% (baseline - 2pp)"
    if_fails: "Fine-tuning catastrophically degraded model"
