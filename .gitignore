# Model checkpoints (large binary files)
/checkpoints/
*.apr

# Training/calibration data
/data/

# Evaluation results (tracked — these are the leaderboard scores)
# results/*.json

# Build artifacts (if any rust remains)
/target/
/models/

# Tool caches
.pmat/
.pmat-work/
.pmat-metrics/

# Documentation build output
docs/book/
